---
data: 2026-09-20
destaque: Modelos Bonsai com o motor da PrismML
---

## Português

- Modelos Ternary Bonsai 2 da PrismML (PTQ1_0, PQ2_0) passam a rodar: o app lê os tipos de tensor do cabeçalho do GGUF, sabe quais arquivos exigem o fork da PrismML do llama.cpp e instala esse motor junto com o download, com um clique.
- O motor troca sob demanda: escolher um Bonsai reinicia o servidor local no motor da PrismML; escolher outro modelo o reinicia na build oficial. A faixa do Servidor Local diz qual está no ar.
- Um Bonsai sem o motor (importado à mão ou instalação que falhou) mostra o botão "Instalar motor PrismML" no chat e na biblioteca em vez de um erro; a gaveta de quantizações avisa antes do download.
- Os rótulos PQ2_0, PTQ1_0, Q2_0 e Q1_0 ganham nome, bits por peso e posição no ranking em vez de "?".
- Apagar um modelo com o servidor no ar descarrega-o na hora e reinicia o servidor quando ocioso, em vez de deixar uma entrada fantasma que respondia HTTP 500.

## English

- PrismML's Ternary Bonsai 2 models (PTQ1_0, PQ2_0) now run: the app reads the tensor types from the GGUF header, knows which files need PrismML's fork of llama.cpp, and installs that engine together with the download, in one click.
- The engine switches on demand: picking a Bonsai model restarts the local server on the PrismML engine; picking another model restarts it on the official build. The Local Server header says which one is running.
- A Bonsai model without the engine (imported by hand or a failed install) shows an "Install PrismML engine" button in the chat and in the library instead of an error; the quantization drawer warns before the download.
- The PQ2_0, PTQ1_0, Q2_0 and Q1_0 labels get a name, bits per weight and a ranking position instead of "?".
- Deleting a model while the server is running unloads it at once and restarts the server when idle, instead of leaving a ghost entry that answered HTTP 500.
