---
data: 2026-09-19
destaque: Jev como camada de decisão para modelos locais
---

## Português

- O Jev (modelo de decisão da TypeSafe, via chave do OpenRouter) decide a cada mensagem se o modelo local deve pensar e quanto — nenhum, médio ou alto — em vez do esforço fixo da conversa. Os detalhes da execução mostram a decisão e a confiança.
- Um proxy local na porta 11712 aplica a mesma decisão às requisições do DeepSeek Harness e dos outros agentes que o app abre. Streaming e demais requisições atravessam intocados; sem decisão, a requisição vai como o agente mandou.
- Cartão novo em Fontes → OpenRouter, desligado por padrão e disponível só com a chave configurada: interruptores para chat e agentes, piso de confiança, teste de decisão, contadores da sessão e aviso do que sai da máquina.
- Só níveis que o chat template do modelo aceita são escritos; modelos sem raciocínio não geram consulta.

## English

- Jev (TypeSafe's decision model, through the OpenRouter key) decides per message whether the local model should think and how hard — none, medium or high — instead of the conversation's fixed effort. Run details show the decision and its confidence.
- A local proxy on port 11712 applies the same decision to requests from the DeepSeek Harness and the other agents the app launches. Streaming and every other request pass through untouched; without a decision, the request goes as the agent sent it.
- New card in Sources → OpenRouter, off by default and available only once the key is set: switches for chat and agents, a confidence floor, a test decision, session counters and a notice of what leaves the machine.
- Only levels the model's chat template accepts are written; models without reasoning never trigger a call.
