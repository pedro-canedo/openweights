---
data: 2026-09-23
destaque: O motor e a GPU funcionam no Linux
---

## Português

- No Linux, a preparação do motor parava em "llama-server não encontrado dentro de llama-…-bin-win-cpu-x64.zip": o app baixava o pacote do llama.cpp feito para o Windows. Agora baixa o do Linux — Vulkan quando há placa de vídeo, CPU quando não há.
- A GPU passa a ser detectada no Linux — NVIDIA pelo driver (NVML), AMD e Intel pelo que o kernel informa —, e a tela de boas-vindas deixa de dizer "Sem GPU dedicada" em máquinas com placa. O monitor de hardware mostra uso, VRAM, temperatura e consumo.
- Placas NVIDIA no Linux rodam o motor oficial pelo Vulkan: a versão do llama.cpp que o app usa não publica CUDA para Linux.
- O limite de energia das placas NVIDIA aparece também no Linux; aplicar um novo pede a senha do sistema.
- O instalador de uma linha deixa o atalho do AppImage com o ícone do app, e a janela aberta fica associada a ele no dock, em vez da engrenagem genérica.

## English

- On Linux, engine setup stopped at "llama-server not found inside llama-…-bin-win-cpu-x64.zip": the app downloaded the llama.cpp package built for Windows. It now downloads the Linux one — Vulkan when there is a graphics card, CPU when there is not.
- GPUs are now detected on Linux — NVIDIA through the driver (NVML), AMD and Intel through what the kernel reports — and the welcome screen no longer says "No dedicated GPU" on machines that have one. The hardware monitor shows usage, VRAM, temperature and power draw.
- NVIDIA cards on Linux run the official engine through Vulkan: the llama.cpp version the app uses does not publish CUDA builds for Linux.
- The power limit of NVIDIA cards now shows on Linux too; applying a new one asks for the system password.
- The one-line installer gives the AppImage shortcut the app icon, and the open window is tied to it in the dock instead of showing the generic gear.
