---
data: 2026-09-14
destaque: Nova tela inicial do Studio
---

## Português

- A tela inicial do Studio ganhou cartões de projetos com modelo base, arquivos e indicação do próximo passo.
- O histórico distingue preparação, treinamento e comparação, com datas, identificação das execuções e modelo utilizado quando disponível.
- Busca por projeto, modelo ou etapa e filtros por modelos prontos, execuções em andamento ou que precisam de atenção facilitam encontrar resultados.
- Modelos prontos podem ser abertos diretamente no chat pelo histórico; na interface independente, o atalho baixa o GGUF.
- A seção “Gerenciar módulo e importar MVP” foi removida da tela. Os dados existentes continuam preservados.
- A navegação permite voltar ao Studio e salva o projeto atual antes de iniciar outro. O layout se adapta a janelas estreitas e aos temas claro e escuro.

## English

- The Studio home screen now uses project cards showing the base model, source files and next step.
- Run history distinguishes preparation, training and comparison, with dates, run identifiers and the model used when available.
- Search by project, model or stage and filter for ready models, active runs or runs needing attention.
- Ready models can be opened directly in chat from history; the standalone interface offers a GGUF download instead.
- The module-management and MVP-import section was removed from this screen. Existing data is preserved.
- Navigation returns to Studio and saves the current project before starting another. The layout supports narrow windows and light and dark themes.
