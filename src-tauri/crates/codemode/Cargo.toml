[package]
name = "lr_codemode"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Code Mode: o modelo escreve um programa que usa as ferramentas, em vez de pedi-las uma a uma"

[dependencies]
lr_types.workspace = true
lr_tools = { path = "../tools" }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
log.workspace = true
tokio.workspace = true
# Só para o token da ponte: 32 bytes do sistema. Já está na árvore (rustls).
getrandom = "0.3"

[dev-dependencies]
tempfile = "3"
